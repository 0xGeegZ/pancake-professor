mutation CreateStrategie($player: String!, $startedAmount: Float!, $maxLooseAmount: Float, $minWinAmount: Float) {
  createStrategie(
    player: $player
    startedAmount: $startedAmount
    maxLooseAmount: $maxLooseAmount
    minWinAmount: $minWinAmount
  ) {
    id
    isActive
  }
}
