mutation CreateStrategie($player: String!, $startedAmount: Int!, $maxLooseAmount: Int, $minWinAmount: Int) {
  createStrategie(
    player: $player
    startedAmount: $startedAmount
    maxLooseAmount: $maxLooseAmount
    minWinAmount: $minWinAmount
  ) {
    id
    isActive
  }
}
