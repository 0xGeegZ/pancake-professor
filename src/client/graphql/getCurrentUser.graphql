query GetCurrentUser {
  currentUser {
    id
    name
    address
    generated
    private
    email
    registeredAt
    loginAt
    strategies {
      id
      player
      amount
      isActive
      isDeleted
      maxLooseAmount
      minWinAmount
    }
  }
}
