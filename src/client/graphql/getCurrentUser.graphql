query GetCurrentUser {
  currentUser {
    id
    name
    address
    generated
    email
    isActivated
    registeredAt
    loginAt
    strategies {
      id
      player
      generated
      startedAmount
      currentAmount
      roundsCount
      playsCount
      isActive
      isRunning
      isDeleted
      isError
      maxLooseAmount
      minWinAmount
      createdAt
    }
  }
}
