query GetCurrentUser {
  currentUser {
    id
    name
    address
    generated
    private
    email
    registeredAt
    loginAt
    strategies {
      id
      player
      startedAmount
      currentAmount
      isActive
      isRunning
      isDeleted
      maxLooseAmount
      minWinAmount
    }
  }
}
