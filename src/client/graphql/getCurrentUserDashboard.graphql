query GetCurrentUserDashboard {
  currentUser {
    id
    name
    address
    kois {
      variety
      id
      birthDate
      breeder
      bloodline
      skinType
      sex
      youtube
      purchasePrice
      updates {
        image
        length
        date
        id
      }
    }
  }
}
