query getFriendKoi($id: String!) {
  user(id: $id) {
    name
    id
    kois {
      variety
      id
      birthDate
      breeder
      bloodline
      skinType
      sex
      youtube
      updates {
        image
        length
        date
        id
      }
    }
  }
}
